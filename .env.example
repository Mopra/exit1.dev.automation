PORT=3000
WEBHOOK_PATH=/webhook
# Leave WEBHOOK_SECRET unset to accept all deliveries (fine for local dev).
# When deployed, set this to a long random string and add the same value to
# the webhook configuration in exit1.dev so the receiver can verify deliveries.
# WEBHOOK_SECRET=replace-me-with-a-long-random-string
